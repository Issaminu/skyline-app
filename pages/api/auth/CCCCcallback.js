import { useRouter } from "next/router";

export default function callback() {
    // const router = useRouter();
    // router.push('/');
    // return (<h1>here is callback land</h1>)
    // callback(null, user, context);

}